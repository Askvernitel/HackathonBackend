{
  "name": "edutech-ai-server",
  "version": "1.0.0",
  "description": "EduTech AI Tutor backend",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "seed": "node src/seed/seed.js",
    "build:client": "cd ../client && npm run build",
    "build": "npm run build:client"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@ngrok/ngrok": "^1.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "localtunnel": "^2.0.2",
    "mongoose": "^8.5.1",
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
